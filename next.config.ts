import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Optimized for Vercel deployment - Next.js will automatically optimize on Vercel
};

export default nextConfig;
